Command line tool for managing js based projects.

node piewpiew new <name>

  Creates a new piewpiew project. Does the following

  # Creates the directory structure
  # Creates a basic package.json file
  # Runs npm install



Intended use:
